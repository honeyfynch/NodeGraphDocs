--[[
  REFERENCE SCAFFOLD — copy to Foundation.

  describe("GraphNodeShell", function()
    local setup = setupTest(GraphNodeShell, { testId = "--foundation-graph-node-shell" })
    it("should create and destroy without errors", ...)
    it("should apply common props", ...) -- FoundationTestingLibrary.Mocks.commonProps
  end)
]]

return {}
